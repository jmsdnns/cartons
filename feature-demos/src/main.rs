#![allow(dead_code, unused_imports)]

mod conversion;
mod enums;
mod inheritence;
mod matching;

fn main() {
    //inheritence::run();
    //conversion::run();
    //matching::run();
    enums::run();
}
