#![allow(dead_code, unused_imports)]

mod conversion;
mod enums;
mod inheritance;
mod iterators;
mod matching;

fn main() {
    //inheritance::run();
    //conversion::run();
    //matching::run();
    //enums::run();
    iterators::run();
}
